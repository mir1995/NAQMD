#ifndef RANDOM_DOT_H
#define RANDOM_DOT_H

/* ---------- Gaussian Distribution ---------
 * Returns a sample from a Gaussian distribution. */
double rand_gaussian_sample(double mean, double std); // free memory 

#endif

