#include "wigner.h" /* Always include the header file that declares something
                     * in the C file that defines it. This makes sure that the
                     * declaration and definition are always in-sync.  Put this
                     * header first in foo.c to ensure the header is self-contained.
                     */
// another way is to define structure data types https://www.geeksforgeeks.org/how-to-return-multiple-values-from-a-function-in-c-or-cpp/
/**
 * This is the function definition.
 * It is the actual body of the function which was declared elsewhere.
 */
// X


double gaussian_get_sample(){
        return 0;
}

void wigner_set_samples(ParticleT *ptr, int npart, int dim){
}
